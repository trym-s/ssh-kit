# Add /snap/bin to PATH only where snap exists (WSL/Ubuntu-snap); no-op elsewhere.
if [[ -d /snap/bin ]]; then
  case ":$PATH:" in
    *:/snap/bin:*) ;;
    *) export PATH="/snap/bin:$PATH" ;;
  esac
fi
